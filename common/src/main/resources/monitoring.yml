slow-query:
  log-path:  ${SLOW_LOG_PATH}/slow.log

app-db-latency:
  monitor:
    enabled: true
    slow-threshold-ms: 300

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
